---
"@modular-prompt/driver": minor
"@modular-prompt/simple-chat": minor
---

simple-chat に `--provider` / `--backend` を追加。ModelSpec にも `backend` フィールドを追加し、MLX 実行モード（auto/lm/vlm/optiq）を provider と分離して統一。
